name: Check Json Validity
on:
  push:
    branches: [ main, master ]
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print check purpose
        run: echo "Pragati Sahayak check-json-validity quality check"
